- **Idea:** Planificación de Arquitectura IA en capas para el próximo sprint (Sprint 14).
  
  ### Detalles del Briefing:
  
  **PRE-REQUISITO — Antes de cualquier tarea técnica:**
  cerrar el sprint 13, que ya está completado y acrchivarlo junto con su investigación y lo mismo con el sprint 12. Ahora empezar el sprint 14.
  
  **BRIEFING DE IMPLEMENTACIÓN — Arquitectura IA en capas (Kanarii)**
  ADRs de referencia: ADR-014 y ADR-015 en `docs/adrs/`. LEER ANTES DE IMPLEMENTAR.
  
  **TAREA 0 — Sanear colisión de numeración ADR**
  Existen dos archivos con el número ADR-013. El más reciente (fecha 2026-06-06) debe renombrarse:
    - ANTES: `ADR-013-firebase-functions-para-og-tags.md`
    - DESPUÉS: `ADR-017-firebase-functions-para-og-tags.md`
  ADR-016 ya está ocupado por el sistema de notificaciones (T-058).
  Actualizar `docs/adrs/README.md` con el índice corregido.
  Commitear: "docs: resolver colisión ADR-013 → ADR-017"
  
  **TAREA 1 — Crear getFichaHash()**
  Ubicación: `src/lib/services/fichaService.ts`
  (ADR-007 dicta que los servicios van por dominio en esta carpeta, NO en el `appService.ts` raíz que está vacío por diseño)
  ```typescript
  export function getFichaHash(ficha: Ficha): string {
    const relevante = {
      rol: ficha.datosPersona?.rol,
      fechaNacimiento: ficha.datosPersona?.fechaNacimiento,
    };
    // btoa es browser-compatible, no depende de Node crypto
    return btoa(JSON.stringify(relevante)).slice(0, 16);
  }
  ```
  
  **TAREA 2 — Refactorizar generarAnalisisCruce (ADR-014 §Cruce)**
  Problema actual: un solo prompt pide JSON + Markdown, separados con regex. Si Gemini omite los backticks → `structured: null as any`.
  
  Separar en dos funciones en `src/lib/gemini.ts`:
  - Capa 1: JSON estructurado, se guarda en Firestore. `kinMayaContext` se inyecta aquí.
    `async function generarCruceInsights(perfil1, perfil2, resultadoDeterminista, comunidadNombre): Promise<CruceInsights>`
    - Output: `{ arquetipo_relacional, clima_grupal_alerta, mapa_rangos, canales_enriquecidos }`
    - Prompt: solo JSON, sin narrativa. `responseSchema` con Gemini structured output si el modelo lo soporta, para eliminar el regex.
  - Capa 2: Narrativa on-demand, NUNCA a Firestore. `kinMayaContext` NO se inyecta (ya está en insights, ahorra ~50% tokens).
    `async function generarCruceNarrativa(insights: CruceInsights, nombres: { persona1: string, persona2: string }): Promise<string>`
    - Prompt incluye literalmente: "Utiliza ESTRICTAMENTE los gancho_proyectivo y necesidad del JSON de contexto. No inventes nuevas áreas de fricción."
  
  Firestore — campos planos en documento existente (NO subcolección):
  `cruces/{id}/`
  - `insights: CruceInsights`
  - `insightsHash: string` // hash de los dos uid implicados + fecha
  
  **TAREA 3 — Refactorizar generarManual (ADR-014 §Manual)**
  Separar en:
  - `generarResumenManual(ficha)` → JSON con 2 frases por pestaña
  - `generarSeccion(tab, resumenCacheado)` → texto largo de esa pestaña
  
  Estado en `ManualViewer.tsx`:
  ```typescript
  const [seccionesGeneradas, setSeccionesGeneradas] = useState<Map<string, string>>(new Map());
  
  // IMPORTANTE: resetear el Map si cambia el uid
  useEffect(() => {
    setSeccionesGeneradas(new Map());
  }, [uid]);
  
  const handleTabClick = async (tab: string) => {
    if (seccionesGeneradas.has(tab)) return; // ya en sesión
    const narrativa = await generarSeccion(tab, resumenCacheado);
    setSeccionesGeneradas(prev => new Map(prev).set(tab, narrativa));
  };
  ```
  
  Lógica de invalidación al entrar a `ManualViewer`:
  ```typescript
  const hashActual = getFichaHash(ficha);
  const regenerar = !fichaDoc.resumenManualHash || fichaDoc.resumenManualHash !== hashActual;
  if (regenerar) {
    const resumen = await generarResumenManual(ficha);
    await updateDoc(fichaRef, {
      resumenManual: resumen,
      resumenManualHash: hashActual
    });
  }
  ```
  
  Firestore — campos planos en `/fichas/{uid}`:
  - `resumenManual: { identidad, poder, sombra, comunicacion, mantenimiento }`
  - `resumenManualHash: string`
  
  **TAREA 4 — PDF opcional (ADR-015 §PDF)**
  Botón "Descargar mi Manual" visible SOLO cuando `seccionesGeneradas.size === TOTAL_TABS` (todas las pestañas generadas).
  Implementar con `jsPDF` + `html2canvas`. Descarga directa desde DOM, sin Firebase Storage.
  
  Campo adicional en `/fichas/{uid}`:
  - `manualPdfUrl: string | null` // el usuario lo gestiona, la app solo lo muestra
  
  ANTES de añadir el campo: verificar en `firestore.rules` que el usuario tiene permiso de escritura en su propio documento `/fichas/{uid}`.
  
  **RESTRICCIONES — no saltarse**
  - `kinMayaContext` solo en Capa 1. Nunca en Capa 2.
  - Narrativas Capa 2 nunca a Firestore.
  - Campos de Capa 1 en el documento existente, no en subcolecciones nuevas.
  - Cualquier decisión que contradiga ADR-014 o ADR-015 debe señalarse explícitamente antes de implementar.
  - Orden: Tarea 0 → Tarea 1 → Tarea 2 → Tarea 3 → Tarea 4
  - Commitear cada tarea por separado.
- **Impacto estimado:** Mucho
- **Contexto:** Trabajando en `docs/sprints/sprint-14.md` → Captura de planificación detallada de la arquitectura de IA en capas para el próximo sprint.
- **Capturado:** 2026-06-07 13:49
